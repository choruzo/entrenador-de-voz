# Dependencias de Python para entrenamiento de Piper TTS
# Optimizado para AMD GPU con ROCm

# Core dependencies
numpy>=1.20.0
scipy>=1.7.0
librosa>=0.9.0
soundfile>=0.11.0

# PyTorch - Instalar con comando específico para ROCm:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm6.0
# torch>=2.0.0  # Comentado - usar el comando de arriba

# ONNX for model export
onnx>=1.12.0
onnxruntime>=1.12.0

# Piper training
# Instalar desde el repositorio:
# cd piper/src/python && pip install -e .
piper-phonemize>=1.1.0

# Data processing
pandas>=1.3.0
tqdm>=4.62.0

# Audio processing utilities
pydub>=0.25.0

# Development tools (optional)
jupyter>=1.0.0
matplotlib>=3.4.0
tensorboard>=2.8.0

# Testing
pytest>=7.0.0
